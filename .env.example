## Frontend (Vite)
# Uses `vite.config.js` proxy in dev; in production, deploy backend separately and set up rewrites.
# (No frontend env vars required for local dev right now)

## Backend (Express)
MONGODB_URI=mongodb://127.0.0.1:27017/noorify
JWT_SECRET=replace-me
# Optional: comma-separated list of allowed origins
# CORS_ORIGIN=http://localhost:5173
PORT=5174

